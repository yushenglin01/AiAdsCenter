# 开发迭代记录

## 基本信息

* 迭代编号：DEV-20260804-008
* 项目版本：0.7.0
* 日期：2026-08-04
* 状态：SUCCESS
* 目标：将 Data、Attribution、Creative、Business、Research、Report、OpenClaw 纳入可运行、可观察的统一系统设计。

## 实现内容

* 泛化 AgentSpec 与 AgentExecutor，新增线程安全 Agent Registry 和七类 Agent 规格。
* Data Agent 重算指标并物化经营规则；Attribution/Creative 执行确定性分析；Business 复用 Outbox/Asynq；Report 读取不可变报告。
* Research 无获批外部连接器时输出 SKIPPED/NOT_CONFIGURED，不伪造市场数据。
* OpenClaw 提供内部 RUN_FULL_ANALYSIS 命令入口，不提供外部消息或广告执行能力。
* 新增 workflow_runs、workflow_steps，保存步骤输入、输出、状态、错误和 Business 外部任务关联。
* 新增 Agent 目录和工作流 API，以及 Vue Agent 工作台、状态轮询和降级提示。

## 接口

* `GET /api/v1/agents`、`GET /api/v1/agents/:name`
* `POST /api/v1/workflows/analysis`
* `GET /api/v1/workflows`、`GET /api/v1/workflows/:id`
* `POST /api/v1/openclaw/commands`

## 数据库

* 新增 `000008_multi_agent_workflows.up.sql/.down.sql`。
* 新增 workflow_runs、workflow_steps。
* agent_tasks 新增 workflow_id、parent_task_id、agent_name；历史任务默认归属 business-agent。

## 验证

* `GOCACHE=/private/tmp/aiadscenter-go-cache go test ./...`：通过。
* `cd web && npm run lint`：通过。
* `cd web && npm test`：2 个测试文件、3 个测试通过。
* `cd web && npm run build`：通过；保留既有大 chunk 非阻塞警告。
* Compose 重建 API/Worker/Web：通过；API、MySQL、Redis 和 Web 健康。
* 真实登录与工作流：Agent 目录返回 7 项，Research=DEGRADED；OpenClaw 启动后 Data/Attribution/Creative/Business/Report 全部 SUCCEEDED，Research=SKIPPED，最终 workflow=COMPLETED。
* 应用内浏览器：登录、PHASE 07 导航、Agent 目录、工作流步骤和降级提示渲染通过，控制台无 warning/error。
* 外部 Research/OpenClaw/Hermes/Adjust/AppsFlyer：NOT_VERIFIED。

## 安全与回滚

* 指标和归因数值仍由确定性代码计算；LLM 只用于 Business 建议。
* 工作流失败会固化失败步骤；所有读取按 tenant_id 过滤。
* 回滚前停止新工作流，备份 workflow_runs/workflow_steps，执行 000008 down 后回退 0.6.0。
