# 开发迭代记录

## 基本信息

* 迭代编号：DEV-20260804-002
* 项目版本：0.2.0
* 开发日期：2026-08-04
* 开发阶段：阶段二：业务目录与数据导入
* 迭代类型：功能开发
* 执行者：Codex
* 关联任务：继续开发 MVP 数据底座
* 关联 Issue：无
* 关联提交：无（当前工作区未初始化 Git 仓库）
* 上一迭代：DEV-20260804-001
* 下一迭代：DEV-20260804-003
* 记录说明：由 DEV-20260804-005 基于当前代码与真实历史验证结果回填。

## 本次目标

实现游戏、渠道、广告计划、素材目录以及广告/MMP/收入/素材指标导入，提供校验、标准化、事务写入与幂等去重。

明确非目标：真实广告平台 API、经营指标计算、Agent、异步导入。

## 开发前状态

* 已存在：阶段一认证、RBAC、单公司边界、Docker 应用壳。
* 未存在：业务目录、事实数据、导入 API 和演示数据。
* 已知问题：数据库仍由 AutoMigrate 启动。
* 测试状态：阶段一验证通过；本阶段功能尚无测试。
* 数据库状态：仅身份四表。
* 接口状态：仅认证、当前公司和权限探针。

## 本次开发内容

### 后端

* 新增 game、campaign/channel、creative、ingestion 模块。
* 新增 CSV、JSON、Mock `DataProvider`，文件与字段校验、国家/币种/日期标准化。
* 导入事务写入 raw/normalized 与三类事实表；文件哈希和业务唯一索引双层去重。

### 前端

* 新增游戏管理、素材管理、数据导入页面与 API 类型。

### 数据库

* 新增 games、channels、campaigns、creatives、data_import_jobs、raw/normalized_ad_metrics、mmp_metrics、game_revenue_metrics、creative_daily_metrics。
* 新增租户范围唯一索引、事实数据去重索引及 soft-delete 索引。

### 接口

* 新增 games/channels/campaigns/creatives CRUD 读取与写入。
* 新增四类 imports 写端点与导入记录读取端点。

### Agent 与 Prompt

* 无。

### 配置与部署

* Demo 数据生成与一键导入脚本扩展。

## 文件变更清单

### 新增文件

* `internal/game/**`、`internal/campaign/**`、`internal/creative/**`、`internal/ingestion/**`
* `pkg/provider/provider.go`、`pkg/provider/provider_test.go`
* `migrations/000002_stage_two.up.sql`、`migrations/000002_stage_two.down.sql`
* `web/src/api/catalog.ts`、`web/src/api/imports.ts`、`web/src/views/GameManagementView.vue`、`AssetManagementView.vue`、`DataImportView.vue`
* `cmd/demo-data/main.go`、`examples/generated/**`、`scripts/import_demo.sh`

### 修改文件

* `internal/bootstrap/router.go`、`storage.go`、`seed.go`
* `web/src/router/index.ts`、`web/src/components/AppShell.vue`

### 删除文件

* 无。

## 接口变更记录

* `GET/POST /api/v1/games`、`GET/PUT /games/:id`：新增；写入 ADMIN/MANAGER，读取登录用户；向后兼容。
* `GET/POST /api/v1/channels`、`GET/PUT /channels/:id`：新增；写入 ADMIN；向后兼容。
* `GET/POST /api/v1/campaigns`、`GET/PUT /campaigns/:id`：新增；写入 ADMIN/MANAGER；向后兼容。
* `GET/POST /api/v1/creatives`、`GET/PUT /creatives/:id`：新增；写入 ADMIN/MANAGER/OPERATOR；向后兼容。
* `POST /api/v1/imports/{ad-metrics,mmp-metrics,game-revenue,creative-metrics}`：新增 JSON/multipart；成功 HTTP 200，校验失败 422，权限失败 403；重复内容幂等返回既有导入结果。
* `GET /api/v1/imports`、`GET /imports/:id`：新增租户范围读取。

## 数据库变更记录

* Migration：`000002_stage_two.up.sql`；回滚：`000002_stage_two.down.sql`。
* 新增表：10 张目录/导入/事实表。
* 索引：公司内业务 ID 唯一、导入幂等、事实日粒度去重、关联读取索引。
* 旧数据兼容：只新增表，不修改身份表。
* 数据回填：无强制回填；Demo 脚本可生成样例。
* 回滚：down 删除新增表，会丢失全部目录和导入事实数据。

## 成功标准

目录 CRUD 与四类导入可运行；非法数据明确失败；重复导入不重复写事实；跨公司查询不可见；构建、单测、Compose 与 E2E 通过。实际达到。

## 失败标准

编译失败、导入错误被吞、重复事实、跨公司访问、事务部分写入、migration 失败时必须失败。实际未触发。

## 验证记录

实际执行并通过：`go test ./...`、`go vet ./...`、前端 build/test、`docker compose up -d --build api web`、`python3 scripts/e2e_stage2.py`。断言总数未留存：`NOT_VERIFIED`。

## 测试结果

### 单元测试

* 总数：NOT_VERIFIED；命令整体通过；失败 0；跳过 NOT_VERIFIED。

### 集成测试

* 总数：1 个阶段脚本；通过 1；失败 0；跳过 0。

### 关键测试场景

* 目录 CRUD、JSON/CSV 导入、非法行拒绝、重复导入幂等、租户条件：通过。
* 数据库中途断开事务：故障注入 NOT_VERIFIED。

## 开发结果

SUCCESS

## 未完成内容

真实广告/MMP API；阶段三至七；异步导入。均为非目标。

## 已知问题

* 数据库仍使用 AutoMigrate；MEDIUM；影响 schema 审计；不阻塞 MVP；DEV-20260804-005 处理。
* 外部平台仅 CSV/JSON/Mock；LOW（范围声明）；不阻塞；真实接入待明确需求后独立迭代。

## 风险说明

* 数据兼容：仅新增表；down 会丢数据。
* 接口兼容：只新增端点。
* 权限/租户：Repository 显式 tenant 条件；服务端 RBAC。
* 幂等：文件哈希与事实唯一索引；同文件内容跨不同请求仍去重。
* 状态一致性：事务内写入；依赖故障注入未验证。
* 模型幻觉：不涉及 LLM。
* 性能：大文件压测 NOT_VERIFIED。

## 回滚方案

停止写入，备份新增表，回退应用与路由。无需删数据时保留表即可；完全回滚时执行 `000002_stage_two.down.sql`，将永久删除目录、导入任务与事实数据。恢复旧镜像和配置后重启并验证阶段一接口。

## 归档内容

* API：`docs/archive/api-contracts/v0.2.0-api-contract.yaml`
* Migration：`migrations/000002_stage_two.*.sql`
* 示例数据：`examples/generated/`
* 测试结果：本记录
* Schema/Prompt：无

## 下一步建议

进入 DEV-20260804-003，基于事实数据实现确定性指标与分析规则。
