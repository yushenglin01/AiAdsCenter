# 开发迭代记录

## 基本信息

* 迭代编号：DEV-20260804-003
* 项目版本：0.3.0
* 开发日期：2026-08-04
* 开发阶段：阶段三：确定性指标与分析
* 迭代类型：功能开发
* 执行者：Codex
* 关联任务：继续开发经营指标、规则、归因与素材分析
* 关联 Issue：无
* 关联提交：无（当前工作区未初始化 Git 仓库）
* 上一迭代：DEV-20260804-002
* 下一迭代：DEV-20260804-004
* 记录说明：由 DEV-20260804-005 基于代码和真实历史验证结果回填。

## 本次目标

用确定性代码计算广告经营指标，实现可配置经营规则、渠道/MMP 归因差异和素材疲劳分析，并提供前端总览与明细。

明确非目标：让 LLM 计算指标、异步流水线、真实广告修改、报告导出。

## 开发前状态

* 已存在：认证、单公司权限、目录、四类事实导入和 Demo 数据。
* 未存在：聚合指标、基准、规则发现、归因和素材风险分析。
* 已知问题：AutoMigrate 尚未替换；真实平台未接入。
* 测试状态：阶段一、二验证通过。
* 数据库状态：身份、目录和事实表存在。
* 接口状态：认证、目录和导入 API 可用。

## 本次开发内容

### 后端

* 新增 decimal 定点指标计算、安全除法和日/计划聚合。
* 新增可配置规则执行、归因差异率、素材疲劳评分及同步分析流水线。
* 归因差异率为 `abs(A-B)/max(abs(A),abs(B))`；素材疲劳为 7 日 CTR 降幅 60% + 频次归一 40%。

### 前端

* 新增经营总览、指标趋势、规则配置、归因异常和素材分析页面及 ECharts 趋势组件。

### 数据库

* 新增 campaign_daily_metrics、business_benchmarks、analysis_rules、rule_findings、attribution_findings、creative_findings。

### 接口

* 新增指标总览、计划列表/详情、趋势、重算、规则读写、归因和素材发现接口。

### Agent 与 Prompt

* 本阶段不调用 LLM；所有结论由确定性规则生成。

### 配置与部署

* Demo 数据扩展为异常、正常和扩量对照样本。

## 文件变更清单

### 新增文件

* `internal/metrics/**`、`internal/rules/**`、`internal/attribution/**`
* `internal/creative/analysis/**`、`internal/analysis/service/pipeline.go`
* `migrations/000003_metrics_and_analysis.up.sql`、`.down.sql`
* `web/src/api/metrics.ts`、`web/src/types/metrics.ts`、`web/src/components/TrendChart.vue`
* `web/src/views/DashboardView.vue`、`MetricsView.vue`、`RulesView.vue`、`AttributionView.vue`、`CreativeAnalysisView.vue`
* `scripts/e2e_stage3.py`

### 修改文件

* `internal/bootstrap/router.go`、`storage.go`、`seed.go`
* `scripts/import_demo.sh`、前端路由、应用壳和样式

### 删除文件

* 无。

## 接口变更记录

* `POST /api/v1/metrics/recalculate?game_id=...`：新增；同步重算并执行三类确定性分析；HTTP 200/403/422；向后兼容。
* `GET /api/v1/metrics/overview`：新增经营总览与风险去重计数。
* `GET /api/v1/metrics/campaigns`、`/:id`、`/trends`：新增聚合、详情和日趋势。
* `GET /api/v1/analysis/rules`、`PUT /rules/:id`：新增；修改仅 ADMIN/MANAGER。
* `GET /api/v1/analysis/attribution`、`/:id`：新增租户范围归因发现。
* `GET /api/v1/analysis/creative`、`/:id`：新增租户范围素材发现。
* 所有变更为新增端点，无删除和不兼容字段变更。

## 数据库变更记录

* Migration：`000003_metrics_and_analysis.up.sql`；回滚：对应 down。
* 新增表：6 张指标、基准、规则与发现表。
* 索引：计划日粒度唯一、公司/游戏/计划/素材查询索引、规则 code 唯一。
* 兼容旧数据：只新增表；需执行重算生成日指标，不修改原始事实。
* 数据回填：Demo 脚本触发重算；生产回填需按游戏执行重算。
* 回滚：删除本阶段表会丢失派生指标和发现，不影响原始导入事实。

## 成功标准

指标可复算、除零安全、规则阈值可配、发现证据可追踪、异常与正常样本可区分；构建、单测、Compose 和 E2E 通过。实际达到。

## 失败标准

LLM 参与关键算数、指标不可复现、重复重算产生重复发现、接口与实现不一致、租户泄漏或 migration 失败时必须失败。实际未触发。

## 验证记录

实际执行并通过：`go test ./...`、`go vet ./...`、前端 build/test、Compose、`python3 scripts/e2e_stage3.py`。单测断言总数未留存：`NOT_VERIFIED`。

## 测试结果

### 单元测试

* 总数：NOT_VERIFIED；命令整体通过；失败 0；跳过 NOT_VERIFIED。

### 集成测试

* 总数：1 个阶段脚本；通过 1；失败 0；跳过 0。

### 关键测试场景

* 指标重算、安全除法、规则命中、归因异常、素材疲劳、正常对照、重复重算去重：通过。
* 性能与大规模回填：NOT_VERIFIED。

## 开发结果

SUCCESS

## 未完成内容

异步工作流、SSE、Agent、报告、真实平台与审批执行；均为后续阶段。

## 已知问题

* 重算与分析同步执行；MEDIUM；影响大数据量响应时间；不阻塞 MVP；阶段五异步化。
* AutoMigrate；MEDIUM；影响迁移审计；DEV-20260804-005 已处理。

## 风险说明

* 数据兼容：派生表可从事实重建；down 不影响事实表。
* 接口兼容：新增端点。
* 权限/租户：查询显式 tenant 条件，规则修改服务端 RBAC。
* 幂等：唯一索引和覆盖式重算；E2E 通过。
* 状态一致性：同步流程中途失败恢复策略有限，阶段五处理。
* 模型幻觉：不使用模型。
* 性能：压测 NOT_VERIFIED。

## 回滚方案

停止重算请求，回退应用。保留派生表可兼容旧应用；需完全回滚时备份后执行 `000003_metrics_and_analysis.down.sql`，只丢失可重建的指标/规则/发现。恢复 0.2.0 镜像并验证导入 API。

## 归档内容

* API：`docs/archive/api-contracts/v0.3.0-api-contract.yaml`
* Migration：`migrations/000003_metrics_and_analysis.*.sql`
* 测试：本记录与 `scripts/e2e_stage3.py`
* Schema/Prompt：无

## 下一步建议

进入 DEV-20260804-004，以确定性结果为输入增加受限 Business Agent。
