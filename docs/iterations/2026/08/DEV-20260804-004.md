# 开发迭代记录

## 基本信息

* 迭代编号：DEV-20260804-004
* 项目版本：0.4.0
* 开发日期：2026-08-04
* 开发阶段：阶段四：受限 Agent 与经营分析
* 迭代类型：功能开发
* 执行者：Codex
* 关联任务：继续开发 Agent、经营建议和审批请求
* 关联 Issue：无
* 关联提交：无（当前工作区未初始化 Git 仓库）
* 上一迭代：DEV-20260804-003
* 下一迭代：DEV-20260804-005
* 记录说明：由 DEV-20260804-005 基于代码、数据库和真实 E2E 输出回填。

## 本次目标

在确定性指标与发现之上实现受限 Business Agent、稳定 Mock LLM、可选 OpenAI-compatible Client、结构化输出校验、建议与高风险审批请求，并提供经营分析页面。

明确非目标：Agent 直接数据库/Shell/任意 HTTP、广告平台修改、异步队列、审批操作、真实外部模型联调。

## 开发前状态

* 已存在：阶段一至三的认证、导入、指标与确定性分析。
* 未存在：AgentSpec、工具注册表、LLM adapter、模型用量、经营建议和审批单。
* 已知问题：同步分析和 AutoMigrate。
* 测试状态：前三阶段通过。
* 数据库状态：指标与发现表存在，无 Agent 表。
* 接口状态：无 Business Agent 接口。

## 本次开发内容

### 后端

* 新增 AgentSpec/AgentExecutor、七个白名单工具、Mock/OpenAI-compatible LLM Client。
* 新增证据、置信度、动作和高风险审批校验；失败最多纠正一次，仍失败进入 MANUAL_REVIEW。
* 新增幂等任务、尝试、模型用量、经营发现、建议与审批请求持久化。

### 前端

* 新增经营分析任务列表、详情、发现、建议和待审批展示。

### 数据库

* 新增 agent_tasks、agent_task_attempts、model_usage_records、business_findings、recommendations、approval_requests。

### 接口

* 新增经营分析创建、任务列表/详情和 Provider 健康/能力端点。

### Agent 与 Prompt

* 新增 Business Agent 与归因/素材/报告边界 Prompt。
* Prompt 版本以文本 `business-v1` 等记录；输出使用旧的最小 JSON Schema 常量。
* 高风险建议只创建 PENDING 审批，不执行外部动作。

### 配置与部署

* 新增 LLM provider、base URL、API Key、model、timeout、prompt dir 配置；默认 Mock。

## 文件变更清单

### 新增文件

* `internal/agent/**`、`internal/llm/**`、`internal/business/**`
* `configs/prompts/*.txt`
* `migrations/000004_agents_and_business.up.sql`、`.down.sql`
* `web/src/api/business.ts`、`web/src/types/business.ts`、`web/src/views/BusinessAnalysisView.vue`
* `scripts/e2e_stage4.py`

### 修改文件

* `internal/config/config.go`、`internal/bootstrap/router.go`、`storage.go`
* `Dockerfile`、`docker-compose.yml`、`.env.example`、`scripts/import_demo.sh`
* 前端路由、应用壳和样式；`README.md`、`docs/api.md`、`docs/architecture.md`

### 删除文件

* 无。

## 接口变更记录

### POST /api/v1/analysis/business

* 变更类型：新增。
* 请求：game_id、campaign_id、可选 analysis_date；Bearer；ADMIN/MANAGER/OPERATOR/ANALYST。
* 成功：HTTP 200，返回同步完成或已存在的幂等任务详情。
* 失败：403、422 或内部错误；校验两次失败时任务为 MANUAL_REVIEW，不伪装成功。
* 兼容性：向后兼容。

### GET /api/v1/analysis/business/tasks、/tasks/:id、/health

* 变更类型：新增；登录用户只读当前公司；200/401/404；向后兼容。

## 数据库变更记录

* Migration：`000004_agents_and_business.up.sql`；回滚：对应 down。
* 新增表：6 张任务、用量、结果和审批表。
* 索引：公司+幂等键唯一；task/game/campaign 状态查询；recommendation 审批唯一。
* 兼容旧数据：仅新增表。
* 数据回填：无。
* 回滚：down 删除 Agent 历史、建议与审批，不影响确定性指标和原始事实。

## 成功标准

受限工具边界成立；LLM 不算关键指标；Mock 可演示；输出必须校验；高风险建议必须审批；幂等任务不重复；单测、构建、Compose、E2E 通过。实际达到。

## 失败标准

模型可直接执行广告修改、访问数据库/Shell、伪造证据、校验失败仍成功、重复任务、migration 失败或真实 Provider 被伪称已联调时必须失败。实际未触发。

## 验证记录

实际执行并通过：`go test ./...`、`go vet ./...`、前端 build/test、Compose、`python3 scripts/e2e_stage4.py`。最后一次 E2E 输出：`stage4_e2e=ok tasks=3`；Meta 任务 SUCCEEDED，6 findings、4 recommendations、2 PENDING approvals；Google 正常对照无发现/审批；重复请求幂等。

## 测试结果

### 单元测试

* 总数：NOT_VERIFIED；命令整体通过；失败 0；跳过 NOT_VERIFIED。

### 集成测试

* 总数：1 个阶段脚本；通过 1；失败 0；跳过 0。

### 关键测试场景

* Mock 结构化输出、Validator、工具权限、Meta 异常、Google 正常、幂等重复请求：通过。
* 真实 OpenAI-compatible 服务：NOT_VERIFIED。
* 数据库不可用/模型限流故障注入：NOT_VERIFIED。

## 开发结果

SUCCESS

## 未完成内容

真实 LLM 联调；Asynq/SSE；审批操作和外部广告执行；完整审计、报告和通知。默认 Provider 明确为 Mock。

## 已知问题

* Prompt 文件未使用语义版本文件名且元数据不完整；MEDIUM；影响可追溯；不阻塞 Mock MVP；DEV-20260804-005 已处理。
* model_usage 缺 prompt_name/schema_version；MEDIUM；影响审计；DEV-20260804-005 已处理。
* 分析同步执行；MEDIUM；影响长响应；阶段五处理。

## 风险说明

* 数据兼容：只新增表。
* 接口兼容：只新增接口。
* 权限/租户：服务端角色与 tenant 过滤；Agent 身份上下文由服务端注入。
* 幂等/状态：唯一键与条件状态转换；E2E 通过。
* 模型幻觉：Validator 与受限工具降低风险，不能完全消除；真实模型未验证。
* 迁移：AutoMigrate 审计风险待处理。
* 性能：同步模型调用压测 NOT_VERIFIED。

## 回滚方案

先把 Provider 切回 Mock 或停止分析入口，备份 Agent 六表，回退 0.3.0 应用。保留表不影响旧版；完全回滚执行 `000004_agents_and_business.down.sql`，会永久删除任务、原始模型输出、用量、建议与审批。API Key 配置从环境移除，不写入日志或数据库。

## 归档内容

* API：`docs/archive/api-contracts/v0.4.0-api-contract.yaml`
* Schema：`docs/archive/schemas/v0.4.0/`
* Prompt：`docs/archive/prompts/v0.4.0/`
* Migration：`migrations/000004_agents_and_business.*.sql`
* 测试：`scripts/e2e_stage4.py` 与本记录

## 下一步建议

先执行 DEV-20260804-005，落地版本、契约、Prompt/Schema 与 migration 归档规约，再进入阶段五。
