# 开发迭代记录

## 基本信息

* 迭代编号：DEV-20260804-001
* 项目版本：0.1.0
* 开发日期：2026-08-04
* 开发阶段：阶段一：项目骨架、认证与单公司边界
* 迭代类型：功能开发
* 执行者：Codex
* 关联任务：创建可运行、可演示、可扩展的海外游戏广告经营智能分析平台 MVP
* 关联 Issue：无
* 关联提交：无（当前工作区未初始化 Git 仓库）
* 上一迭代：无
* 下一迭代：DEV-20260804-002
* 记录说明：本记录由 DEV-20260804-005 根据代码、migration 与本次会话中的真实验证结果回填；缺失数据标记为 `NOT_VERIFIED`。

## 本次目标

建立 Go/Vue/Docker Compose 项目骨架，实现单公司用户名密码登录、JWT 刷新、服务端 RBAC、当前公司读取和租户范围查询基础，为后续业务模块提供可运行底座。

明确非目标：多公司管理、真实广告平台、经营指标、Agent、异步任务。

## 开发前状态

* 已存在的功能：空项目目录，无可运行应用。
* 未存在的功能：后端、前端、数据库、认证、权限和部署均不存在。
* 当前已知问题：无代码可验证。
* 当前测试状态：无测试。
* 当前数据库状态：无 schema。
* 当前接口状态：无接口。

## 本次开发内容

### 后端

* 建立 Gin、GORM、Viper、Zap、JWT 应用骨架。
* 实现登录、刷新、当前用户、当前公司与 ADMIN 权限探针。
* 登录不接受公司参数，由服务端固定公司；JWT 保留 `tenant_id` 安全边界。
* 增加请求 ID、CORS、鉴权和 RBAC 中间件。

### 前端

* 建立 Vue 3、TypeScript、Vite、Pinia、Vue Router 与 Element Plus 应用。
* 实现登录页、应用壳、权限路由和管理探针页面。

### 数据库

* 新增 tenants、users、roles、user_roles。
* 新增公司内用户名唯一约束和角色关联主键。
* 新增 `000001_init.up.sql` 与 `000001_init.down.sql`。

### 接口

* 新增 `/health`、`/api/v1/auth/login`、`/refresh`、`/auth/me`、`/tenants/current`、`/admin/ping`。

### Agent 与 Prompt

* 无。

### 配置与部署

* 新增 Dockerfile、Docker Compose、环境配置、API/Worker/Web 容器和 Demo seed。

## 文件变更清单

### 新增文件

* `cmd/server/main.go`、`cmd/worker/main.go`
* `internal/auth/**`、`internal/tenant/**`、`internal/middleware/**`、`internal/common/**`
* `internal/bootstrap/**`、`internal/config/config.go`
* `migrations/000001_init.up.sql`、`migrations/000001_init.down.sql`
* `web/src/App.vue`、`web/src/stores/auth.ts`、`web/src/views/LoginView.vue`
* `Dockerfile`、`docker-compose.yml`、`scripts/start.sh`、`scripts/test.sh`

### 修改文件

* 无（首次建立）。

### 删除文件

* 无。

## 接口变更记录

### POST /api/v1/auth/login

* 变更类型：新增。
* 请求变化：`username`、`password`；不接收 tenant/company 字段。
* 成功响应：HTTP 200，JWT Access/Refresh Token。
* 失败响应：HTTP 401。
* 兼容性：向后兼容（首次发布）。

### POST /api/v1/auth/refresh

* 变更类型：新增；成功 HTTP 200，失败 HTTP 401；首次发布。

### GET /api/v1/auth/me、GET /api/v1/tenants/current、GET /api/v1/admin/ping

* 变更类型：新增。
* 请求变化：Bearer Token；admin/ping 需要 ADMIN。
* 成功响应：HTTP 200；失败响应：401/403。
* 兼容性：向后兼容（首次发布）。

## 数据库变更记录

* Migration：`000001_init.up.sql`；回滚：`000001_init.down.sql`。
* 新增表：tenants、users、roles、user_roles。
* 索引：tenant slug、role code、`tenant_id + username` 唯一；user_roles 复合主键。
* 旧数据兼容：首次建库，无历史数据。
* 数据回填：Demo seed，不是生产回填。
* 是否可回滚：可；按 down 文件逆序删除四表，会删除身份数据。

## 成功标准

* 后端和前端可构建；登录、刷新、鉴权、RBAC 和公司范围接口可用。
* migration 可执行，Docker Compose 可启动。
* 不把单公司体验描述为已交付多租户产品。
* 实际结果：达到；历史归档在 DEV-20260804-005 补齐。

## 失败标准

项目无法编译、认证绕过、非 ADMIN 可访问管理探针、跨公司查询、migration 或 Compose 无法运行时必须失败。实际未触发。

## 验证记录

实际执行并通过：`GOCACHE=/tmp/gai-go-cache go test ./...`、`go vet ./...`、`cd web && npm run build && npm test`、`docker compose up -d --build api web`、`python3 scripts/e2e_stage1.py`。具体单测断言总数未从历史输出保存：`NOT_VERIFIED`。

## 测试结果

### 单元测试

* 总数：NOT_VERIFIED
* 通过：命令整体通过
* 失败：0（按命令退出状态）
* 跳过：NOT_VERIFIED

### 集成测试

* 总数：1 个阶段脚本
* 通过：1
* 失败：0
* 跳过：0

### 关键测试场景

* 正常登录、Token 刷新、无权限访问、当前公司读取：通过。
* 跨租户访问：通过服务端默认公司和 tenant 条件验证；完整多租户矩阵不适用。
* 数据库不可用：健康检查降级逻辑存在，故障注入 `NOT_VERIFIED`。

## 开发结果

SUCCESS

## 未完成内容

阶段二至七业务能力；真实外部平台；Worker 异步执行。均属于明确非目标。

## 已知问题

* 问题描述：数据库启动仍依赖 GORM AutoMigrate；严重等级：MEDIUM；影响：迁移审计；不阻塞本版演示；建议：改为版本化 SQL runner；预计：DEV-20260804-005（已处理）。

## 风险说明

* 数据兼容/迁移：首次建库；down 会删除身份数据。
* 接口兼容：首次契约，无删除。
* 权限/租户隔离：服务端 RBAC 与 tenant 条件是强制边界；前端守卫不是安全边界。
* 幂等/状态一致性/模型幻觉：本阶段不涉及 Agent；seed 设计为幂等。
* 性能：MVP 数据量，未进行压测，`NOT_VERIFIED`。

## 回滚方案

停止 api/web，回退 0.1.0 代码与配置；若允许删除本版所有身份数据，人工执行 `000001_init.down.sql`。保留数据时不得执行 down，只回退应用。恢复旧环境变量并重建容器。执行 down 会永久丢失 users/roles/tenants，必须先备份。

## 归档内容

* OpenAPI：`docs/archive/api-contracts/v0.1.0-api-contract.yaml`
* Migration：`migrations/000001_init.*.sql` 与归档清单
* 测试结果：本记录
* JSON Schema、Prompt：本阶段无

## 下一步建议

进入 DEV-20260804-002，实现游戏、渠道、计划、素材与数据导入。
