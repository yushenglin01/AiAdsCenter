# 开发迭代记录

## 基本信息

* 迭代编号：DEV-20260804-009
* 项目版本：0.8.0
* 日期：2026-08-04
* 状态：SUCCESS
* 目标：使七 Agent 工作流具备安全重试、实时状态和持久化内部消息能力。

## 实现内容

* workflow_runs 增加数据库唯一幂等键；并发冲突回读已有工作流。
* 新增 Workflow SSE；Vue 使用 AbortController、状态更新和 5 秒退避重连。
* 新增 workflow_notifications、通知 Repository/Service/Handler 与租户隔离 API。
* 完成、待审批、失败、人工复核、取消生成一次内部消息；标记已读操作幂等。
* Agent 工作台新增 OpenClaw Inbox、未读数和消息状态操作。

## 接口与数据库

* `GET /api/v1/workflows/:id/events`
* `GET /api/v1/notifications?status=UNREAD|READ`
* `POST /api/v1/notifications/:id/read`
* `000009_workflow_idempotency_and_notifications.up.sql/.down.sql`

## 验证

* `GOCACHE=/private/tmp/aiadscenter-go-cache go test ./...`：通过。
* `cd web && npm run lint && npm test && npm run build`：通过；2 个测试文件、5 个测试。
* Compose 重建 API/Worker/Web 并应用 000009：通过；API/MySQL/Redis/Web 健康，Worker 运行。
* 两次相同 OpenClaw 命令均返回 `c4395daa-8caf-4eeb-a761-39db5d6d0910`；最终 COMPLETED，Research=SKIPPED，其余六步 SUCCEEDED。
* Workflow SSE 返回 1 个 workflow 事件；该 workflow 只生成 1 条内部通知。
* 同一通知连续两次标记已读均返回 READ，验证操作幂等。
* 应用内浏览器：PHASE 08、7 个 Agent、OpenClaw Inbox、已读消息和完整步骤渲染通过；控制台无 warning/error。

## 安全与回滚

* 通知读写显式使用 tenant_id，内部消息不声称外部投递成功。
* 系统仍没有广告平台写入 Tool；WAITING_APPROVAL 不代表建议已执行。
* 回滚前停止新工作流并备份 workflow_runs/workflow_notifications；执行 000009 down 后回退 0.7.0。
