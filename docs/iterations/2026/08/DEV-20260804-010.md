# 开发迭代记录

## 基本信息

* 迭代编号：DEV-20260804-010
* 项目版本：0.9.0
* 日期：2026-08-04
* 状态：SUCCESS
* 目标：建立首个真实只读 MMP 数据闭环，使 AppsFlyer 数据可安全进入现有指标与 Agent 工作流。

## 实现内容

* 新增租户/游戏级 AppsFlyer App ID 映射；Token 仅从服务端环境变量读取。
* 接入 Raw Data Pull API v5 installs 与 in-app events，Bearer 鉴权、8 秒超时、429/5xx 有限重试。
* 按日期、campaign_id、country_code 确定性聚合 installs、activations、唯一 payers 和 USD revenue。
* 复用 MMP 导入校验；同步按连接/App ID/日期范围幂等，并事务替换同一范围以吸收延迟事件回补。
* 新增 mmp_connections、mmp_sync_runs、同步错误分类、审计事件和租户隔离 API。
* 数据导入页增加连接健康、App ID 配置、最多 7 天手工同步和同步记录。

## 接口与数据库

* `GET /api/v1/mmp-connections`
* `PUT /api/v1/mmp-connections/appsflyer`
* `GET /api/v1/mmp-sync-runs`
* `POST /api/v1/mmp-connections/:id/sync`
* `000010_appsflyer_mmp_sync.up.sql/.down.sql`

## 验证

* `GOCACHE=/private/tmp/aiadscenter-go-cache go vet ./...`：通过。
* `GOCACHE=/private/tmp/aiadscenter-go-cache go test ./...`：通过。
* `cd web && npm run lint && npm test && npm run build`：通过；3 个测试文件、7 个测试。
* AppsFlyer Client 单测覆盖 Bearer、路径与查询参数、429 重试、CSV 聚合、唯一付费人数、负收入拒绝和 Token 不泄漏。
* MMP Service 单测覆盖未配置降级、成功导入、同步幂等和安全错误持久化。
* Compose 重建并应用 000010：API/MySQL/Redis/Web 健康，Worker 运行；migration 与配置审计记录已核对。
* 应用内浏览器真实登录：PHASE 09、映射保存、NOT_CONFIGURED 降级、同步禁用正确；控制台无 warning/error。
* 未配置真实 Token，因此真实 AppsFlyer 账号联调状态为 NOT_VERIFIED。

## 安全与回滚

* Token 不进入 API、数据库、审计和错误消息；Base URL 固定为官方 HTTPS 主机。
* 20 万行或 50MB 达到上限时拒绝导入，缺少 campaign_id 的行计数告警；不存在的本地计划使整次导入失败。
* 连接配置限 ADMIN/MANAGER，同步限 ADMIN/MANAGER/OPERATOR；所有查询按 tenant_id 隔离。
* 回滚前停止新同步并备份 mmp_connections/mmp_sync_runs；执行 000010 down 后回退 0.8.0。已导入 mmp_metrics 保留，需按 import_job_id 单独治理。
