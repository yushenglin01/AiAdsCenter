# 版本 1.1.1

* 发布日期：2026-08-08
* 包含迭代：DEV-20260808-001（SUCCESS）
* 可靠性：分析窗口新增租约和 claim token，可恢复 Worker 崩溃并隔离旧执行结果。
* 数据校验：金额与频次在入库前按 MySQL DECIMAL 列范围和 6 位小数精度拒绝非法输入。
* 数据库变更：`000013_analysis_window_leases` 为 analysis_windows 增加 locked_until、claim_token 与恢复扫描索引。
* 兼容性：HTTP 路径、标准批次 schema 名称和版本保持不变；现有 1.1.0 数据卷可前向升级。
* 已知限制：真实 Kafka Broker 联调、日期范围增量分析和租约时长压测仍依赖目标环境。
