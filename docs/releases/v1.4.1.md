# 版本 1.4.1

* 发布日期：2026-08-10
* 包含迭代：DEV-20260810-002、DEV-20260810-003（SUCCESS）
* 数据接入：AppsFlyer/Adjust 连接新增明确的待验证状态；只有完成真实上游拉取和确定性导入后才显示已验证。
* 前端：待验证连接可以直接执行首次同步，状态使用中文标签并显示最近成功同步时间。
* 可靠性：重复请求命中仍在处理的同步运行时，界面不再误报同步完成。
* 契约：`health` 明确为 `NOT_CONFIGURED`、`UNVERIFIED`、`READY`、`DISABLED` 四态。
* 数据库：无 migration；状态由现有连接、部署级配置和 `last_sync_at` 派生。
* 安全性：API 仍只暴露凭证配置布尔值，不返回 Token、Adjust 指标 slug 或上游原始错误正文。
* Agent：Research、Creative 和 OpenClaw 可使用统一真实 LLM；Report 摘要润色默认关闭。所有增强均受输入引用校验、模型溯源和确定性回退保护。
* OpenClaw：增加白名单自然语言解析；启动分析必须二次确认，结构化命令保持兼容。
* 已知限制：未提供真实 AppsFlyer/Adjust 凭证，本版本不声称完成真实账号端到端数值核对；READY 表示至少一次历史同步成功，不是持续在线探测。
* LLM 限制：未提供真实模型 API Key，本版本不声称完成真实模型端到端响应或费用核对；Creative 尚无媒体资产输入。
