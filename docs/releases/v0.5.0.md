# 版本 0.5.0

* 发布日期：2026-08-04
* 包含迭代：DEV-20260804-006（SUCCESS）
* 新增功能：事务 Outbox、Asynq Worker、任务重试/心跳租约、SSE 状态流、确定性 Markdown 报告快照与下载。
* 修改功能：经营分析由同步执行改为 HTTP 202 接收；页面恢复后自动继续监听未完成任务。
* 修复问题：修复数据库/Redis 双写缺口、Worker 崩溃残留 RUNNING、重试产生部分重复结果和重复投递副作用。
* 不兼容变更：`POST /api/v1/analysis/business` 成功状态码从 200 改为 202；客户端必须等待终态。Go 构建环境从 1.22+ 提升到 1.24+。
* 数据库变更：`000006_async_workflow_and_reports` 为 agent_tasks 增加队列字段，并创建 task_outbox、analysis_reports。
* 接口变更：新增 `/tasks/:id/events` 和 `/tasks/:id/report`；任务详情新增 queue_retry_count、queued_at、last_heartbeat_at 和 report。
* Prompt/Schema：无变更，继续使用 business-agent-output 1.0.0 与现有 1.0.0 Prompt。
* 部署要求：API 与 Worker 都需要 Redis 和相同队列配置；API 先完成 migration，Worker 再启动。Asynq 版本固定为 v0.26.0。
* 升级步骤：备份数据库；部署 0.5.0 API 并确认 000006；部署 Worker；检查 task_outbox 无长期 PENDING；验证提交返回 202、SSE 和报告。
* 回滚步骤：先停止新任务提交和 Worker；等待或记录未完成任务；备份 task_outbox/analysis_reports；执行 000006 down 并删除对应 schema_migrations 记录；回退 0.4.1。未完成异步任务和报告将丢失。
* 已知问题：Asynq 公共 API 仍为 v0，升级需回归；真实 OpenAI-compatible 模型、跨进程 OpenTelemetry、审批操作和生产告警 NOT_VERIFIED。
* 验收结果：Go 单测/vet、前端 lint/test/build、归档检查、Compose migration/健康、阶段四回归、阶段五 E2E、Worker 离线恢复均通过。
