# 版本 0.8.0

* 发布日期：2026-08-04
* 包含迭代：DEV-20260804-009（SUCCESS）
* 新增功能：工作流 SSE、OpenClaw 内部消息、未读/已读交互。
* 可靠性：完整分析增加数据库级幂等键，并发重试返回同一工作流。
* 数据库变更：`000009_workflow_idempotency_and_notifications` 增加 workflow_runs.idempotency_key 与 workflow_notifications。
* 接口变更：新增 `/workflows/:id/events`、`/notifications`、`/notifications/:id/read`。
* 兼容性：原 Agent、Workflow、Business、报告和审批接口保持可用。
* 已知限制：外部消息、Research、Adjust/AppsFlyer 和外部 OpenClaw/Hermes 仍未配置。
* 安全边界：内部消息按 tenant_id 隔离，不包含广告平台执行能力，外部投递状态明确为 NOT_CONFIGURED。
* 验收结果：全量测试与归档校验、Compose 000009、重复命令、SSE、通知唯一性、重复已读和浏览器可视化检查全部通过。
