# 版本 0.1.0

* 发布日期：2026-08-04
* 包含迭代：DEV-20260804-001（SUCCESS）
* 新增功能：项目骨架、JWT 登录/刷新、RBAC、当前公司、Docker Compose 和 Demo 用户。
* 修改功能/修复问题：无，首次发布。
* 不兼容变更：无。
* 数据库变更：`000001_init` 新增 tenants/users/roles/user_roles。
* 接口变更：新增认证、当前用户/公司、健康和 ADMIN 探针。
* Prompt/Schema：无。
* 部署要求：Docker Compose、MySQL 8、Redis 7；更换 JWT secret。
* 升级步骤：配置环境变量，构建并启动 Compose，验证 `/health` 和登录。
* 回滚步骤：回退镜像；只有允许删除全部身份数据时执行 `000001_init.down.sql`。
* 已知问题：启动依赖 AutoMigrate（0.4.1 已移除）。
* 验收结果：阶段一 E2E、后端测试、静态检查和前端构建通过；断言总数 NOT_VERIFIED。
