# 版本 1.1.0

* 发布日期：2026-08-05
* 包含迭代：DEV-20260805-002（SUCCESS）
* 数据接入：新增标准批次 1.0.0，HTTP 与 Kafka 支持 AF、Adjust、广告平台和自有业务数据。
* 可靠性：新增 MySQL Inbox、载荷哈希冲突检测、手动 Offset、DLQ 与分析窗口版本认领。
* 数据库变更：`000012_standard_ingestion_kafka` 新增 ingestion_messages、analysis_windows 和导入批次溯源字段。
* 部署：新增可选 ingestion-worker 与 Compose `kafka` profile；Kafka Broker 由外部环境提供。
* 安全性：消费者固定 tenant/producer，TLS 默认开启，原始载荷不写日志，REPLACE_RANGE 仅限 MMP。
* 已知限制：尚未完成真实 Kafka 环境联调、Schema Registry、SCRAM/OAuth 和日期范围增量分析。
