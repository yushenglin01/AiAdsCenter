# 版本 0.7.0

* 发布日期：2026-08-04
* 包含迭代：DEV-20260804-008（SUCCESS）
* 新增功能：七类 Agent 注册目录、持久化工作流、OpenClaw 内部命令入口、Agent 工作台。
* 数据库变更：`000008_multi_agent_workflows` 新增 workflow_runs、workflow_steps，并给 agent_tasks 增加 workflow_id、parent_task_id、agent_name。
* 接口变更：新增 `/agents`、`/workflows`、`/workflows/analysis`、`/openclaw/commands`。
* 兼容性：原 `/analysis/business`、SSE、报告和审批接口保持可用。
* 已知限制：Research、外部 OpenClaw/Hermes、消息推送和真实 Adjust/AppsFlyer API 尚未配置；状态会明确显示 DEGRADED/NOT_CONFIGURED。
* 安全边界：没有广告平台写入工具，Research 无来源时不生成事实，所有工作流查询按 tenant_id 隔离。
* 验收结果：Go 测试、Vue 类型检查、Vitest、生产构建、归档校验、Compose 000008 migration、真实登录与七 Agent 工作流通过；外部连接器联调未执行。
